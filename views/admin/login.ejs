<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin Girişi | Auto Akın</title>
    <link rel="stylesheet" href="/css/panel.css?v=3.1" />
</head>

<body class="auth-page">
    <div class="auth-card">
        <div style="text-align:center;margin-bottom:32px;">
            <a href="/" class="sidebar-logo" style="justify-content:center;transform:none;opacity:1;">
                <div class="brand-mark">A</div>
                <div style="text-align:left;">
                    <span>Auto Akın</span>
                    <small>Yönetim Paneli</small>
                </div>
            </a>
        </div>
        <h1>Panel Girişi</h1>
        <p class="auth-sub">Yönetim paneline erişmek için giriş yapın</p>
        <div class="auth-error" id="authError"></div>
        <form onsubmit="doLogin(event)">
            <div class="form-group">
                <label>E-posta</label>
                <input type="email" id="adminEmail" placeholder="admin@autoakin.com" required />
            </div>
            <div class="form-group">
                <label>Şifre</label>
                <input type="password" id="adminPassword" placeholder="••••••••" required />
            </div>
            <button class="btn btn-block" type="submit" style="margin-top:8px;">Giriş Yap</button>
        </form>
        <p style="text-align:center;margin-top:24px;">
            <a href="/"
                style="color:var(--text-muted);font-weight:700;font-size:13px;text-decoration:none;transition:0.3s;"
                onmouseover="this.style.color='var(--primary)'" onmouseout="this.style.color='var(--text-muted)'">←
                Siteye Dön</a>
        </p>
    </div>
    <script>
        async function doLogin(e) {
            e.preventDefault();
            const r = await fetch('/api/auth/admin-login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: document.getElementById('adminEmail').value, password: document.getElementById('adminPassword').value }) });
            const d = await r.json();
            if (d.error) { document.getElementById('authError').textContent = d.error; document.getElementById('authError').style.display = 'block'; }
            else window.location.href = '/panel/dashboard';
        }
    </script>
</body>

</html>